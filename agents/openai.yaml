interface:
  display_name: "Build SailfishOS"
  short_description: "Build SailfishOS RPMs with Docker MB2."
  default_prompt: "Use $build-sailfishos to build this SailfishOS project for a chosen release and architecture."

policy:
  allow_implicit_invocation: true
